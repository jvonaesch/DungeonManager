package dungeonmanager.feature;

/**
 * Only holds the name, description and ID of a feature when the inheritors when parent
 * creature already applied all effects of the feature, so that the feature can still
 * be displayed in the session
 */
public class DummyFeature {

}
